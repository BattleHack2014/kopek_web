db_default:
    host: localhost
    dbname: goal
    user: goal
    password: goal
    driver: pdo_mysql
    charset: utf8
    driverOptions:
        PDO::ATTR_ERRMODE: PDO::ERRMODE_EXCEPTION

db_master:
    host: localhost
    dbname: goal
    user: goal
    password: goal
    driver: pdo_mysql
    charset: utf8
    driverOptions:
        PDO::ATTR_ERRMODE: PDO::ERRMODE_EXCEPTION

db_slave:
    host: localhost
    dbname: goal
    user: goal
    password: goal
    driver: pdo_mysql
    charset: utf8
    driverOptions:
        PDO::ATTR_ERRMODE: PDO::ERRMODE_EXCEPTION

